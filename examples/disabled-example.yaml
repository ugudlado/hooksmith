# Any rule type can be disabled without removing it
rules:
  - name: disabled-example
    on: PreToolUse Bash
    match: command =~ echo hello
    context: "This rule is disabled and won't fire."
    enabled: false
