# match — Protect sensitive file types from writes
rules:
  - name: protected-files
    on: PreToolUse Write|Edit
    match: file_path =~ \.(env|ssh|pem)$
    deny: "Protected file: cannot write to .env, .ssh, or .pem files."
